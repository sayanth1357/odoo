<?xml version="1.0" encoding="utf-8"?>
<odoo>

     <record id="action_recurring_subscription_wizard" model="ir.actions.act_window">
        <field name="name">Subscription Report</field>
        <field name="res_model">recurring.subscription.wizard</field>
         <field name="target">new</field>
        <field name="view_mode">form</field>
    </record>

       <record id="recurring_subcription_wizard_view" model="ir.ui.view">
           <field name="name">recurring.subscription.wizard</field>
           <field name="model">recurring.subscription.wizard</field>
           <field name="arch" type="xml">
               <form string="Report">
                   <group>
                       <field name="date_from"/>
                       <field name="date_to"/>
                       <field name="subscription_id"/>
                       <field name="report"/>
                       <field name="partner_id"/>
                   </group>
                   <footer>
                       <button string="Print PDF" name="action_report_subscription" type="object"  class="btn-primary"/>
                       <button string="Cancel"  type="object" special="cancel"/>
                   </footer>
               </form>
           </field>
       </record>
</odoo>