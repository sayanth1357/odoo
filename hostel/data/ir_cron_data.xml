<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_auto_create_invoice" model="ir.cron">
            <field name="name">Auto create invoice : Start of month</field>
            <field name="model_id" ref="model_hostel_room"/>
            <field name="state">code</field>
            <field name="code">model._cron_create_invoice()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="nextcall" eval="(DateTime.now().replace(day=1,hour=0,minute=0,second=0) + relativedelta(months=1))"/>
            <field name="active">True</field>
            <field name="priority">1</field>
        </record>
        <record id="ir_cron_auto_create_cleaning_request" model="ir.cron">
            <field name="name">Auto create Cleaning Request : Day wise</field>
            <field name="model_id" ref="model_cleaning_service"/>
            <field name="state">code</field>
            <field name="code">model._cron_create_daily_request()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=6,minute=0,second=0) + relativedelta(days=1))"/>
            <field name="active">True</field>
            <field name="priority">1</field>
        </record>
    </data>
</odoo>